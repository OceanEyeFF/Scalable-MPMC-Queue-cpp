# LSCQ C++ 性能验证与Benchmark方案

> 基于论文Section 7的系统化性能测试设计
>
> 文档版本: v1.0
> 创建日期: 2026-01-17
> 作者: Scaleable-MPMC-Queue-cpp Team

---

[TOC]

---

## 1. 概述

### 1.1 性能验证目标

1. **正确性验证**: 确保算法在高并发下无data race和livelock
2. **性能基准**: 与论文结果对比，达到80%以上性能
3. **可扩展性验证**: 验证性能随核数增加而提升
4. **内存效率**: 对比LCRQ等算法的内存占用
5. **跨平台验证**: 在x86-64、ARM64等平台上的表现

### 1.2 参考基准

基于论文Section 7的benchmark设计，我们将对比：

| 算法 | 说明 | 用途 |
|------|------|------|
| **LSCQ** | 我们的实现 | 主要测试对象 |
| **MSQueue** | M&S lock-free queue | 经典baseline |
| **LinkedQueue** | Mutex + 双向链表 | 锁实现baseline |
| **Folly MPMC** | Facebook的MPMC queue | 工业级对比 |
| **Boost lockfree** | Boost的无锁队列 | 开源对比 |

*注：LCRQ和WFQUEUE需要特殊依赖，暂不对比*

### 1.3 测试环境规范

#### 1.3.1 硬件配置

**主测试平台** (对标论文的Intel Xeon):
- CPU: Intel Xeon或AMD EPYC (至少16核)
- 内存: 32GB+
- 操作系统: Ubuntu 22.04 LTS

**次要测试平台**:
- ARM64: AWS Graviton或Apple M系列
- CPU核数: 8-16核

#### 1.3.2 编译配置

```bash
# Release构建 (关键!)
cmake -DCMAKE_BUILD_TYPE=Release \
      -DCMAKE_CXX_FLAGS="-O3 -march=native" \
      -DLSCQ_BUILD_BENCHMARKS=ON \
      ..

make -j$(nproc)
```

**编译器**:
- GCC 11+ 或 Clang 14+
- 开启`-O3`优化
- 使用`-march=native`充分利用CPU特性

---

## 2. Benchmark场景设计

### 2.1 场景分类

基于论文Section 7，我们设计了以下6类benchmark场景：

| 场景 | 说明 | 论文对应 |
|------|------|----------|
| EmptyQueue | 空队列dequeue | Figure 11 |
| EnqueueDequeue Pair | 成对操作 | Figure 13a, 14a |
| 50E50D | 随机50%入队50%出队 | Figure 13b, 14b |
| 70E30D | 随机70%入队30%出队 | - |
| 30E70D | 随机30%入队70%出队 | - |
| MemoryEfficiency | 内存占用测试 | Figure 12 |

### 2.2 场景详细设计

#### 2.2.1 EmptyQueue - 空队列测试

**目的**: 测试dequeue在空队列时的开销

```cpp
// benchmark_empty_queue.cpp
static void BM_EmptyQueue_Dequeue(benchmark::State& state) {
    int thread_count = state.range(0);
    LSCQ<uint64_t> queue;  // 空队列

    // 每个线程不断dequeue
    auto worker = [&]() {
        for (auto _ : state) {
            auto val = queue.dequeue();
            benchmark::DoNotOptimize(val);
        }
    };

    std::vector<std::thread> threads;
    for (int i = 0; i < thread_count; ++i) {
        threads.emplace_back(worker);
    }

    for (auto& t : threads) {
        t.join();
    }

    // 报告吞吐量 (Mops/sec)
    state.SetItemsProcessed(state.iterations() * thread_count);
}

BENCHMARK(BM_EmptyQueue_Dequeue)
    ->Arg(1)->Arg(2)->Arg(4)->Arg(8)->Arg(16)->Arg(32)
    ->UseRealTime();
```

**关键指标**:
- 吞吐量 (Mops/sec)
- 延迟 (ns/op)

**预期结果** (基于论文Figure 11a):
- LSCQ应与MSQueue、NCQ性能相近
- 不应像LCRQ、WFQUEUE那样出现性能下降

#### 2.2.2 Pair - 成对入队出队

**目的**: 测试紧密耦合的enqueue-dequeue操作

```cpp
static void BM_EnqueueDequeue_Pair(benchmark::State& state) {
    int thread_count = state.range(0);
    LSCQ<uint64_t> queue;

    auto worker = [&]() {
        for (auto _ : state) {
            queue.enqueue(42);
            auto val = queue.dequeue();
            benchmark::DoNotOptimize(val);
        }
    };

    std::vector<std::thread> threads;
    for (int i = 0; i < thread_count; ++i) {
        threads.emplace_back(worker);
    }

    for (auto& t : threads) {
        t.join();
    }

    state.SetItemsProcessed(state.iterations() * thread_count * 2);  // 每次2个操作
}

BENCHMARK(BM_EnqueueDequeue_Pair)
    ->Arg(1)->Arg(2)->Arg(4)->Arg(8)->Arg(16)->Arg(32)->Arg(72)
    ->UseRealTime();
```

**预期结果** (基于论文Figure 13a):
- LSCQ应与LCRQ性能相近 (论文中两者几乎一致)
- 大幅优于MSQueue和LinkedQueue
- 核数增加时性能应线性或超线性增长

#### 2.2.3 50E50D - 平衡负载

**目的**: 模拟生产者和消费者数量相当的场景

```cpp
static void BM_50Enqueue50Dequeue(benchmark::State& state) {
    int thread_count = state.range(0);
    LSCQ<uint64_t> queue;

    std::atomic<bool> stop{false};
    std::atomic<uint64_t> enqueued{0};
    std::atomic<uint64_t> dequeued{0};

    auto worker = [&]() {
        std::mt19937 rng(std::random_device{}());
        std::uniform_int_distribution<int> dist(0, 1);

        for (auto _ : state) {
            if (dist(rng) == 0) {
                // 50%概率enqueue
                queue.enqueue(42);
                enqueued.fetch_add(1, std::memory_order_relaxed);
            } else {
                // 50%概率dequeue
                auto val = queue.dequeue();
                if (val != EMPTY_VALUE) {
                    dequeued.fetch_add(1, std::memory_order_relaxed);
                }
            }
        }
    };

    std::vector<std::thread> threads;
    for (int i = 0; i < thread_count; ++i) {
        threads.emplace_back(worker);
    }

    for (auto& t : threads) {
        t.join();
    }

    state.SetItemsProcessed(enqueued.load() + dequeued.load());
}

BENCHMARK(BM_50Enqueue50Dequeue)
    ->Arg(1)->Arg(2)->Arg(4)->Arg(8)->Arg(16)->Arg(32)
    ->UseRealTime();
```

**预期结果** (基于论文Figure 13b):
- LSCQ应优于或持平于LCRQ
- 显著优于MSQueue和LinkedQueue

#### 2.2.4 30E70D - Dequeue密集

**目的**: 测试Go实现中的fixstate优化效果

```cpp
static void BM_30Enqueue70Dequeue(benchmark::State& state) {
    // 类似50E50D，但概率改为30/70
    // ...
}

BENCHMARK(BM_30Enqueue70Dequeue)
    ->Arg(1)->Arg(2)->Arg(4)->Arg(8)->Arg(16)->Arg(32)
    ->UseRealTime();
```

**预期结果**:
- 验证我们的catchup优化 (应有15-30%提升)
- 对比未优化版本

#### 2.2.5 70E30D - Enqueue密集

```cpp
static void BM_70Enqueue30Dequeue(benchmark::State& state) {
    // 类似50E50D，但概率改为70/30
    // ...
}
```

#### 2.2.6 Memory Efficiency - 内存占用

**目的**: 对比不同算法的内存使用

```cpp
#include <sys/resource.h>

void measure_memory_usage() {
    struct rusage usage;
    getrusage(RUSAGE_SELF, &usage);
    return usage.ru_maxrss;  // KB
}

static void BM_Memory_50E50D(benchmark::State& state) {
    int thread_count = state.range(0);

    size_t mem_before = measure_memory_usage();

    // 运行50E50D场景10秒
    auto start = std::chrono::steady_clock::now();
    while (std::chrono::steady_clock::now() - start < std::chrono::seconds(10)) {
        // ... 运行测试
    }

    size_t mem_after = measure_memory_usage();
    state.counters["MemoryMB"] = (mem_after - mem_before) / 1024.0;
}
```

**预期结果** (基于论文Figure 12b):
- LSCQ应保持稳定的内存占用 (< 10MB)
- 避免LCRQ的内存暴涨问题 (论文中达400MB)

---

## 3. 性能指标定义

### 3.1 主要指标

#### 3.1.1 吞吐量 (Throughput)

**定义**: 每秒完成的操作数

**计量单位**: Mops/sec (Million operations per second)

**计算方式**:
```
Throughput = Total_Operations / Total_Time (seconds)
```

**目标值** (基于论文Figure 13a, 16核):
- LSCQ Pair: > 50 Mops/sec
- LSCQ 50E50D: > 40 Mops/sec

#### 3.1.2 延迟 (Latency)

**定义**: 单次操作的耗时

**计量单位**: nanoseconds (ns)

**计算方式**:
```
Latency = Total_Time / Total_Operations
```

**目标值**:
- Enqueue/Dequeue: < 50 ns (单核)
- Enqueue/Dequeue: < 100 ns (16核)

#### 3.1.3 可扩展性 (Scalability)

**定义**: 性能随核数增加的提升比例

**计算方式**:
```
Speedup(N) = Throughput(N_cores) / Throughput(1_core)
```

**目标值**:
- 线性可扩展: Speedup(N) ≈ N
- 实际目标: Speedup(16) > 10 (考虑NUMA等因素)

#### 3.1.4 内存占用 (Memory Usage)

**定义**: 算法运行时的峰值内存

**计量单位**: MB

**目标值**:
- SCQ: 固定 < 5MB
- LSCQ: < 20MB (稳态)

### 3.2 次要指标

- **Cache Miss Rate**: 使用`perf stat`测量
- **Branch Mispredictions**: CPU分支预测失败率
- **Lock Contention**: 对比算法的锁竞争情况

---

## 4. 对比基准实现

### 4.1 MSQueue (M&S Lock-Free Queue)

**实现来源**: 自行实现或使用Boost.Lockfree

```cpp
#include <boost/lockfree/queue.hpp>

using MSQueue = boost::lockfree::queue<uint64_t>;
```

### 4.2 LinkedQueue (Mutex + Linked List)

**简单实现**:

```cpp
template<typename T>
class LinkedQueue {
    std::mutex mutex_;
    std::deque<T> deque_;

public:
    void enqueue(T val) {
        std::lock_guard<std::mutex> lock(mutex_);
        deque_.push_back(val);
    }

    std::optional<T> dequeue() {
        std::lock_guard<std::mutex> lock(mutex_);
        if (deque_.empty()) return std::nullopt;
        T val = deque_.front();
        deque_.pop_front();
        return val;
    }
};
```

### 4.3 Folly MPMC Queue

**实现来源**: Facebook Folly库

```cpp
#include <folly/MPMCQueue.h>

folly::MPMCQueue<uint64_t> queue(65536);
```

---

## 5. 测试流程

### 5.1 自动化Benchmark脚本

**run_benchmarks.sh**:

```bash
#!/bin/bash

# 设置CPU频率为固定值（避免Turbo Boost影响）
echo "Setting CPU governor to performance..."
sudo cpupower frequency-set -g performance

# 禁用超线程 (可选)
echo 0 | sudo tee /sys/devices/system/cpu/cpu*/online

# 运行benchmark
echo "Running benchmarks..."
./benchmark_all --benchmark_out=results.json \
                 --benchmark_out_format=json \
                 --benchmark_repetitions=10

# 生成报告
python3 scripts/analyze_results.py results.json

# 恢复CPU设置
sudo cpupower frequency-set -g ondemand
```

### 5.2 结果分析脚本

**analyze_results.py**:

```python
import json
import matplotlib.pyplot as plt

def plot_throughput(data):
    """绘制吞吐量对比图"""
    algorithms = ['LSCQ', 'MSQueue', 'LinkedQueue', 'NCQ']
    cores = [1, 2, 4, 8, 16, 32]

    for alg in algorithms:
        throughputs = extract_throughput(data, alg)
        plt.plot(cores, throughputs, label=alg, marker='o')

    plt.xlabel('Number of Cores')
    plt.ylabel('Throughput (Mops/sec)')
    plt.title('Throughput vs Cores - EnqueueDequeue Pair')
    plt.legend()
    plt.grid(True)
    plt.savefig('throughput_comparison.png', dpi=300)

def calculate_speedup(data):
    """计算可扩展性"""
    baseline = data['1_core']
    for cores in [2, 4, 8, 16, 32]:
        speedup = data[f'{cores}_core'] / baseline
        print(f"{cores} cores: {speedup:.2f}x")

# ... 更多分析函数
```

---

## 6. 性能目标矩阵

基于论文Figure 13a (Pair, 16核)和我们的保守估计：

| 算法 | 吞吐量目标 | 内存占用 | 可扩展性 (16核) |
|------|-----------|----------|----------------|
| **LSCQ** | > 50 Mops/sec | < 5 MB | > 10x |
| MSQueue | ~ 4 Mops/sec | < 1 MB | < 2x |
| LinkedQueue | ~ 12 Mops/sec | < 2 MB | < 1.5x |
| NCQ | ~ 10 Mops/sec | < 5 MB | < 5x |

*注：目标基于论文Intel Xeon E7-8880结果，我们的实现目标是达到80%*

---

## 7. 回归测试

### 7.1 性能回归检测

在每次PR合并前运行benchmark，确保性能无退化：

```yaml
# .github/workflows/benchmark.yml
name: Performance Regression

on: [pull_request]

jobs:
  benchmark:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout base
        run: git checkout ${{ github.base_ref }}
      - name: Run baseline benchmark
        run: ./build/benchmark_all --benchmark_out=baseline.json

      - name: Checkout PR
        run: git checkout ${{ github.head_ref }}
      - name: Run PR benchmark
        run: ./build/benchmark_all --benchmark_out=pr.json

      - name: Compare results
        run: python3 scripts/compare_benchmarks.py baseline.json pr.json
```

### 7.2 性能退化阈值

如果PR导致任何测试场景性能下降超过**5%**，则CI失败，需要人工审查。

---

## 8. 性能分析工具

### 8.1 Linux Perf

**使用方式**:

```bash
# 记录性能事件
perf record -g ./benchmark_pair

# 查看报告
perf report

# 统计缓存未命中
perf stat -e cache-misses,cache-references ./benchmark_pair
```

**关注指标**:
- Cache miss rate < 5%
- Branch mispredictions < 2%
- Instructions per cycle > 1.5

### 8.2 Intel VTune

**适用场景**: 深度性能分析

**使用步骤**:
1. 运行Hotspots分析
2. 识别热点函数
3. 检查微架构瓶颈
4. 优化关键路径

### 8.3 FlameGraph

**生成火焰图**:

```bash
perf record -F 99 -g ./benchmark_all
perf script | flamegraph.pl > flamegraph.svg
```

---

## 9. 测试报告模板

### 9.1 性能测试报告结构

```
# LSCQ性能测试报告

## 测试环境
- CPU: Intel Xeon ...
- 内存: 32GB
- 操作系统: Ubuntu 22.04
- 编译器: GCC 11.3

## 测试结果摘要
- 吞吐量: XX Mops/sec (达到目标的X%)
- 可扩展性: XX.Xx倍加速 (16核)
- 内存占用: XX MB

## 详细数据
[表格: 不同场景下的性能数据]

## 与论文对比
[对比图: 我们的实现 vs 论文结果]

## 性能瓶颈分析
[Perf分析结果]

## 优化建议
[...]
```

---

## 10. 验收标准总结

### 10.1 必须达成 (Must Have)

1. ✅ **功能正确性**: 所有单元测试通过
2. ✅ **无内存泄漏**: Valgrind检测通过
3. ✅ **无data race**: ThreadSanitizer通过
4. ✅ **性能基线**: 达到论文结果的**80%**以上

### 10.2 期望达成 (Should Have)

1. ⭐ 性能达到论文结果的**90%**以上
2. ⭐ 可扩展性: 16核加速比 > 12x
3. ⭐ 内存占用 < 3MB (稳态)

### 10.3 可选达成 (Nice to Have)

1. 🎯 性能超越论文结果
2. 🎯 支持ARM64平台并性能相近
3. 🎯 提供详细的性能调优文档

---

**文档变更记录**:

| 版本 | 日期 | 作者 | 变更内容 |
|------|------|------|----------|
| v1.0 | 2026-01-17 | Team | 初始版本，定义性能验证方案 |
